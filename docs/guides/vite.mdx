---
title: Vite
description: Vite에서 Jotai 사용하는 방법
nav: 8.99
keywords: vite
published: false
---

Vite와 Jotai를 함께 사용할 때는 `jotai-babel` 패키지의 플러그인을 사용해 개발 경험을 향상시킬 수 있습니다.

`vite.config.ts`에서는 다음과 같이 설정합니다.

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import jotaiDebugLabel from 'jotai-babel/plugin-debug-label'
import jotaiReactRefresh from 'jotai-babel/plugin-react-refresh'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    react({ babel: { plugins: [jotaiDebugLabel, jotaiReactRefresh] } }),
  ],
  // ... 나머지 설정
})
```

아래 템플릿으로 직접 시험해 볼 수 있습니다.

### 예제

#### Vite 템플릿

<Stackblitz id="vitejs-vite-xhhxve" file="vite.config.ts" />
